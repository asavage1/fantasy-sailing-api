var assert = require("assert");

// if (process.env.NODE_ENV != "test") {
// 	process.exit(1);
// }

describe("GET /", function () {
	describe("balh", function () {
		it("should pass", function () {
			assert.equal(true, true);
		});
	});
});
